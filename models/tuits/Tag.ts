/**
 * @file Declares Tag data type representing the tuit's tag.
 */

/**
 * @typedef Tag Represents the tuit's tag.
 * @property {string} tag Name of the tag.
 */
export default class Tag {
    private tag: string = '';
}
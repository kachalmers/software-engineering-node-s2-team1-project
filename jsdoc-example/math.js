/**
 * @file Implements arithmetic functions
 */

/**
 * Adds a plus b
 * @param {number} a Augend parameter
 * @param {number} b Addend parameter
 * @returns {number} Sum of a and b
 */
function add(a, b) {
  return a + b;
}